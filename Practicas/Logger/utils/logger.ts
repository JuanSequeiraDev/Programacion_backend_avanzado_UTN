/* import filesystem from 'fs' */

/* export class Log{
    module: string
    message: string
    timestamp: Date
    id: number
    level: 'debug' | 'info' | 'warn' | 'error'

    constructor(id: number, message: string, module: string, timestamp: Date, level: 'debug' | 'info' | 'warn' | 'error'){
        this.id = id
        this.message = message
        this.module = module
        this.timestamp = timestamp
        this.level = level
    }
}

export class LoggerManager{
    logs: []
    id_counter: number

    constructor(){
        this.logs = []
        this.id_counter = 0
    }

    addLog(level: 'debug' | 'info' | 'warn' | 'error', module: string, method: string){
        
    }
} */