

{{!-- <form method="POST" action="/product/new">
    <div>
        <label for="nombre">Ingrese nombre: </label>
        <input placeholder="tv samsung" id="nombre" name="nombre" value="{{this.data.form_state.nombre.valor}}" />
        {{#if this.data.form_state.nombre.error}}
        <span style="color: red;">ERROR: {{this.data.form_state.nombre.error}}</span>
        {{/if}}
    </div>
    <div>
        <label for="precio">Ingrese precio: </label>
        <input id="precio" name="precio" value="{{this.data.form_state.precio.valor}}" />
        {{#if this.data.form_state.precio.error}}
        <span style="color: red;">ERROR: {{this.data.form_state.precio.error}}</span>
        {{/if}}
    </div>
    <div>
        <label for="stock">Ingrese stock: </label>
        <input id="stock" name="stock" value="{{this.data.form_state.stock.valor}}" /> {{!-- El name es el nombre del
        valor en el backend --}}
        {{#if this.data.form_state.stock.error}}
        <span style="color: red;">ERROR: {{this.data.form_state.stock.error}}</span>
        {{/if}}
    </div>
    <div>
        <label for="categoria">Seleccione una de las categorias disponibles</label>
        <br>
        <span>Tecnologia</span>
        <input type="checkbox" value="tecnologia" name="categoria">
        <br>
        <span>Hogar</span>
        <input type="checkbox" value="hogar" name="categoria">
        <br>
        <span>Office</span>
        <input type="checkbox" value="office" name="categoria">
        <br>
    </div>
    <div>
        <label for="descripcion">Ingrese descripcion: </label>
        <input id="descripcion" name="descripcion" value="{{this.data.form_state.descripcion.valor}}">
        </input>
        {{#if this.data.form_state.descripcion.error}}
        <span style="color: red;">ERROR: {{this.data.form_state.descripcion.error}}</span>
        {{/if}}
    </div>

    <input type="submit" value="Crear producto">
</form> --}}

<h1>Cargar producto</h1>

<form method="POST" action="/product/new">
    {{#each this.data.form_state}}
        <div>
            <label for="{{this.name}}">{{this.label}}</label>
            {{#if this.isInput}}
                <input 
                    placeholder="{{this.placeholder}}" 
                    id="{{this.name}}" 
                    name="{{this.name}}"
                    value="{{this.valor}}" 
                    type="{{this.type}}"
                />
            {{/if}}
            {{#if this.isTextarea}}
                <textarea 
                    placeholder="{{this.placeholder}}" 
                    id="{{this.name}}" 
                    name="{{this.name}}"
                    value="{{this.valor}}" 
                >
                    {{this.valor}}
                </textarea>
            {{/if}}
            {{#if this.errors.length}}
                <ul>
                    {{#each this.errors}}
                        <li>
                            {{this.message}}
                        </li>
                    {{/each}}
                </ul>
            {{/if}}
        </div>

    {{/each}}